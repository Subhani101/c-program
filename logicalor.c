#include <stdio.h>

int main() {
    
    // int issunday,issnowing;
    // printf("Enter the value of issunda : \n");
    // scanf("%d",issunday);
    // printf("Enter the value of is issnowing \n :");
    // scanf("%d",issnowing);
    // printf("%d", issnowing || issunday);
    // printf("%d",issnowing&&issunday);
int Sunday,Snowing;
Sunday=0;
Snowing=0;
printf("%d",Sunday||Snowing);
    return 0;
}